// Re-export from shared so renderer imports keep working unchanged.
// Canonical source: src/shared/markdown.ts (used by both renderer and main).
export { renderMarkdown } from '@shared/markdown.js';
